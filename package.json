{
  "name": "student-gwa-calculator-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "cd StudentGWACalculator && npm install",
    "build": "cd StudentGWACalculator && npm run build",
    "vercel-build": "npm run build"
  }
}
